<!--
GENERATION PROMPT: produce ONE synthetic retelling of a base news text from a
given angle, to simulate a different person recounting the same real events.
The different angles deliberately induce the phenomena the aggregation pipeline
must handle: omitted events, renamed/re-worded events, different granularity, and
occasional added detail. Vars: {{TEXT}}, {{PERSPECTIVE}}.
-->
You are simulating how different people recount the same real events.

Rewrite the news text below as {{PERSPECTIVE}}.

Keep the SAME underlying real events and their real outcome — do not invent a
contradictory ending. But recount them naturally in your own words: it is expected
that this version mentions a different subset of the events, uses different wording
or a different level of detail, and may order things differently from the original.
Write 1-4 sentences of plain narrative prose — no lists, no headings, no commentary.

Original text:
"""{{TEXT}}"""
